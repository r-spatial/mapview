mapview (upcoming release)
--------------------------------------------------------------------------------

new features:
  * projView
  * knitr integration
  * cubeView
  * labels: if zcol is set, mouseover will now show the repesctive values of zcol, if zcol is not set moseover shoes feature ID.
  * new popup functions popupTable and popupGraph

changes:
  * colors: viridis based colors now default if package is available

bugfixes:
  * if attribute was of class "character" mapview did through an error if passed to zcol.


mapview 1.0.0
--------------------------------------------------------------------------------

* Initial release
